# Coursiv 功能实现方案

**版本**: 1.1  
**日期**: 2026年1月11日  
**状态**: 待确认

---

## 1. 当前实现状态

| 模块 | 状态 | 说明 |
|-----|------|------|
| 主落地页 | ✅ 完成 | 14个Section，包含动画和交互 |
| Onboarding Quiz | ✅ 完成 | 22步问卷流程 |
| Demo 登录 | ✅ 完成 | 临时登录系统 |
| Dashboard 框架 | ✅ 完成 | 5个内页基础布局 |
| Upsell 页面 | ✅ 完成 | 付费引导页 |

---

## 2. 待实现功能清单

---

### 迭代一：用户系统升级 + 测试模式基础设施

**功能说明**：替换 Demo 登录，实现真实的邮箱密码登录系统，并建立测试模式基础设施供后续迭代使用。

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| 邮箱密码登录 | 真实数据 | 邮箱、密码哈希存储到数据库 |
| 用户资料 | 真实数据 | 用户名、邮箱、头像URL |
| 忘记密码 | 模拟 | 显示提示页面，不发送真实邮件 |
| **测试模式开关** | 配置 | 环境变量 + UI 开关 |

**测试模式设计**：

| 层级 | 配置 | 说明 |
|-----|------|------|
| 环境变量 | `ALLOW_TEST_MODE=true/false` | 生产环境设为 false，完全禁用测试模式 |
| UI 开关 | Settings → Developer Options | 仅当环境变量允许时显示 |
| 状态提示 | 顶部横幅 | 🧪 Test Mode Enabled |

**测试模式下的行为变化**（供后续迭代使用）：

| 功能 | 正式模式 | 测试模式 |
|-----|---------|----------|
| Streak 增加 | 每天一次 | 每次学习都 +1 |
| Streak 重置 | 隔天归零 | 不重置 |
| 证书解锁阈值 | 100% | 10% |
| 课程进度 | 逐页阅读 | 可一键完成模块 |
| 挑战任务 | 逐天完成 | 可批量完成 |
| AI 积分 | 每次扣减 | 不扣减 |

**完成度检查**:
- [x] 登录页面 UI 与原版一致
- [x] 注册流程：Quiz 完成后设置密码
- [x] 密码加密存储 (bcrypt)
- [x] JWT Token 认证
- [x] 登出功能
- [ ] "Forgot password?" 链接（跳转提示页）
- [x] 环境变量 `ALLOW_TEST_MODE` 配置
- [x] Settings 页面 "Developer Options" 区块（仅当允许时显示）
- [x] "Enable Test Mode" 开关
- [x] 测试模式状态全局存储（Context/Store）
- [x] 顶部 🧪 横幅提示（测试模式开启时）

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|----------|
| 注册流程 | 完成 Quiz → 输入邮箱密码 → 提交 | 用户创建成功，自动登录 |
| 登录流程 | 输入已注册邮箱密码 → 点击登录 | 登录成功，跳转 Dashboard |
| 登录失败 | 输入错误密码 | 显示错误提示 |
| 登出 | 点击 Log Out | 清除 Token，跳转首页 |
| 密码加密 | 查看数据库 | 密码字段为哈希值，非明文 |
| 测试模式开关 | ALLOW_TEST_MODE=true 时访问 Settings | 显示 Developer Options |
| 测试模式开关隐藏 | ALLOW_TEST_MODE=false 时访问 Settings | 不显示 Developer Options |
| 测试模式横幅 | 开启测试模式 | 顶部显示 🧪 横幅 |

**自动化测试用例**:
```typescript
// 测试文件: server/auth.test.ts
- test('用户注册成功')
- test('用户登录成功')
- test('错误密码登录失败')
- test('登出清除Token')

// 测试文件: client/src/__tests__/TestMode.test.tsx
- test('环境变量允许时显示Developer Options')
- test('环境变量禁止时隐藏Developer Options')
- test('开启测试模式显示横幅')
```

---

### 迭代二：Weekly Streaks 系统

**功能说明**：帮助用户建立学习习惯的连续学习追踪系统。

| 功能 | 数据类型 | 正式模式 | 测试模式 |
|-----|---------|---------|---------|
| 连续学习天数 | 真实数据 | 每天学习一次 +1 | 点击任意课程 +1 |
| Streak 重置 | 真实逻辑 | 隔天未学习归零 | 不自动重置 |
| 7天日历视图 | 真实数据 | 显示本周记录 | 可手动勾选 |
| 火焰图标 | 真实数据 | 显示当前天数 | 实时更新 |

**完成度检查**:
- [ ] Home 页面显示 "WEEKLY STREAKS" 卡片
- [ ] 7天日历视图 (Sat-Fri)
- [ ] 已完成天数显示勾选标记
- [ ] 导航栏显示火焰图标 + 连续天数
- [ ] 数据库表 user_streaks 创建
- [ ] 学习行为触发 streak 更新
- [ ] 测试模式开关

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| Streak 增加 | 测试模式下点击课程内容 | streak +1，日历当天打勾 |
| Streak 显示 | 查看导航栏 | 火焰图标 + 正确天数 |
| 日历视图 | 查看 Home 页面 | 7天日历，已学习天打勾 |
| 数据持久化 | 刷新页面 | streak 数据保持 |

**自动化测试用例**:
```typescript
// 测试文件: server/streaks.test.ts
- test('学习行为增加streak')
- test('获取用户streak数据')
- test('streak数据持久化')
```

---

### 迭代三：课程数据结构

**功能说明**：创建课程数据模型，填充模拟课程内容。

| 数据 | 类型 | 数量 | 说明 |
|-----|------|-----|------|
| 课程列表 | 模拟数据 | 9个 | ChatGPT, DALL-E, Midjourney, Claude, DeepSeek, Stable Diffusion, Gemini, Jasper AI, Lovable |
| 课程模块 | 模拟数据 | ~150个 | 每课程15-20个模块 |
| 课程内容 | 模拟数据 | ~500页 | 每模块3-5页，每页200-500字 |
| 用户进度 | 真实数据 | - | 存储用户学习进度 |

**模拟数据详情**:

| 课程 | 模块数 | 内容示例 |
|-----|-------|---------|
| ChatGPT | 13个 | "ChatGPT as an LLM", "Intro to Prompting", "Discovering Modes & Features"... |
| DALL-E | 16个 | "Introduction to DALL-E", "Text-to-Image Basics", "Advanced Prompting"... |
| Midjourney | 15个 | "Getting Started", "Understanding Parameters", "Style References"... |
| ... | ... | ... |

**完成度检查**:
- [ ] 数据库表 courses 创建
- [ ] 数据库表 course_modules 创建
- [ ] 数据库表 course_content 创建
- [ ] 数据库表 user_progress 创建
- [ ] 9个课程数据填充
- [ ] 每个课程包含模块数据
- [ ] 每个模块包含内容数据
- [ ] Guides 页面显示课程列表

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| 课程列表 | 访问 Guides 页面 | 显示9个课程卡片 |
| 课程详情 | 点击课程卡片 | 显示模块列表 |
| 数据完整性 | API 查询 | 返回完整课程数据 |

**自动化测试用例**:
```typescript
// 测试文件: server/courses.test.ts
- test('获取课程列表')
- test('获取课程模块')
- test('获取模块内容')
```

---

### 迭代四：课程学习路径视图

**功能说明**：以树状结构展示课程模块和学习进度。

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| 路径树布局 | 模拟数据 | 固定坐标的节点位置 |
| 节点内容 | 模拟数据 | 来自课程模块数据 |
| 进度显示 | 真实数据 | 用户实际完成百分比 |
| 证书卡片 | 模拟数据 | 锁定状态样式 |

**完成度检查**:
- [ ] 课程详情页路由 `/guides/:courseId`
- [ ] 顶部课程选择下拉菜单
- [ ] 紫色标题栏 (AI MASTERY · LEVEL X + 课程名)
- [ ] 左侧证书卡片（锁定图标 + 进度条）
- [ ] 右侧学习路径树（节点式布局）
- [ ] 节点之间用曲线/直线连接
- [ ] 点击节点弹出详情弹窗
- [ ] 弹窗包含: 标题、描述、Read/Listen 按钮
- [ ] 进度百分比实时更新

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| 路径视图 | 点击 Guides 中的课程 | 显示树状路径视图 |
| 节点弹窗 | 点击任意节点 | 弹出详情弹窗 |
| 进度显示 | 完成部分内容后查看 | 进度百分比更新 |
| 课程切换 | 使用下拉菜单切换 | 显示不同课程路径 |

**自动化测试用例**:
```typescript
// 测试文件: client/src/__tests__/CoursePathway.test.tsx
- test('渲染课程路径树')
- test('节点点击弹出弹窗')
- test('进度百分比正确显示')
```

---

### 迭代五：课程内容阅读页

**功能说明**：展示课程具体内容的阅读界面。

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| 课程文字 | 模拟数据 | 预设的AI工具介绍，每页200-500字 |
| 课程插图 | 模拟数据 | SVG图标或渐变色占位图 |
| 进度条 | 真实数据 | 当前页/总页数 |
| 音频按钮 | 模拟 | 点击提示 "Coming Soon" |
| 学习进度 | 真实数据 | 自动保存到数据库 |

**模拟内容示例**:
```
标题: ChatGPT's Brain

正文:
ChatGPT runs on something called a Large Language Model (LLM). 
It sounds complicated, but don't worry!

Let's sort out how it works.

Basically, an LLM is like a super-smart librarian who has read 
every book, article, and webpage, capable of understanding and 
generating human-like text. Why "large"? Because LLMs are trained 
on massive amounts of data.

As a result, by learning patterns and structures in language, 
they can create coherent and contextually appropriate responses.
```

**完成度检查**:
- [ ] 阅读页路由 `/guides/:courseId/:moduleId/:contentId`
- [ ] 顶部关闭按钮 (X)
- [ ] 横向进度条
- [ ] 音频开关按钮（点击提示 Coming Soon）
- [ ] 课程标题显示
- [ ] 课程正文（支持加粗、段落）
- [ ] 插图展示
- [ ] 底部 "Continue" 按钮
- [ ] 右下角反馈按钮
- [ ] 退出确认弹窗 "Wait, don't go!"
- [ ] 自动保存学习进度到数据库
- [ ] 完成内容后触发 streak 更新

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| 内容显示 | 点击 Read 按钮 | 显示课程内容页 |
| 翻页 | 点击 Continue | 进入下一页内容 |
| 进度条 | 翻页过程中观察 | 进度条正确更新 |
| 退出弹窗 | 点击 X 按钮 | 弹出确认弹窗 |
| 进度保存 | 完成几页后刷新 | 进度保持 |
| Streak 触发 | 完成一个模块 | streak +1 |

**自动化测试用例**:
```typescript
// 测试文件: client/src/__tests__/LessonContent.test.tsx
- test('渲染课程内容')
- test('Continue按钮翻页')
- test('退出弹窗显示')
- test('进度条更新')
```

---

### 迭代六：课程内 Quiz

**功能说明**：课程模块中的测验问题。

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| Quiz 问题 | 模拟数据 | 每模块1-2个选择题 |
| 选项 | 模拟数据 | 4个选项 A/B/C/D |
| 正确答案 | 模拟数据 | 预设正确选项 |
| 答题记录 | 真实数据 | 存储用户答题结果 |

**模拟数据示例**:
```json
{
  "question": "What does LLM stand for?",
  "options": [
    "Large Language Model",
    "Linear Learning Machine",
    "Language Logic Module",
    "Learning Language Method"
  ],
  "correctIndex": 0,
  "explanation": "LLM stands for Large Language Model, which is the technology behind ChatGPT."
}
```

**完成度检查**:
- [ ] Quiz 页面 UI 与原版一致
- [ ] 问题标题显示
- [ ] 4个选项卡片
- [ ] 选择后显示正确/错误反馈
- [ ] 正确答案高亮（绿色）
- [ ] 错误答案标红
- [ ] 显示解释文字
- [ ] "Continue" 按钮进入下一内容
- [ ] Quiz 结果记录到数据库

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| Quiz 显示 | 完成课程内容后 | 显示 Quiz 页面 |
| 选择正确 | 点击正确选项 | 绿色高亮 + 成功提示 |
| 选择错误 | 点击错误选项 | 红色标记 + 显示正确答案 |
| 继续 | 点击 Continue | 进入下一内容 |

**自动化测试用例**:
```typescript
// 测试文件: client/src/__tests__/Quiz.test.tsx
- test('渲染Quiz问题和选项')
- test('选择正确答案反馈')
- test('选择错误答案反馈')
```

---

### 迭代七：挑战系统

**功能说明**：展示挑战列表和每日任务。

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| 挑战列表 | 模拟数据 | 4个挑战 |
| 每日任务 | 模拟数据 | 14-28天任务列表 |
| 任务进度 | 真实数据 | 用户完成状态 |

**模拟数据**:

| 挑战名称 | 天数 | 难度 | 任务示例 |
|---------|-----|------|---------|
| AI Reinvention 2026 Challenge | 28天 | Beginner | Day 1: DeepSeek, Day 2: ChatGPT... |
| Junior AI Challenge | 28天 | Beginner | Day 1: ChatGPT Basics... |
| 14-Day AI Side Gigs Challenge | 14天 | Beginner | Day 1: Freelancing with AI... |
| No Code Challenge | 14天 | Beginner | Day 1: Intro to No-Code... |

**完成度检查**:
- [ ] Challenges 页面显示挑战列表
- [ ] "All" / "Completed" 筛选按钮
- [ ] "My challenges" 区块（进行中的挑战）
- [ ] 挑战卡片（封面图、名称、天数、难度）
- [ ] 挑战详情页路由 `/challenges/:challengeId`
- [ ] 顶部天数导航 (D1-D28 横向滚动)
- [ ] 挑战介绍和进度百分比
- [ ] 证书卡片
- [ ] 每日任务列表（图标 + 名称 + 状态）
- [ ] "Start now" 按钮
- [ ] 任务完成状态保存到数据库

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| 挑战列表 | 访问 Challenges 页面 | 显示4个挑战 |
| 挑战详情 | 点击挑战卡片 | 显示详情页 |
| 天数导航 | 点击 D1-D28 | 滚动到对应天数 |
| 任务完成 | 点击任务 | 标记完成，保存到数据库 |
| 筛选功能 | 点击 Completed | 显示已完成挑战 |

**自动化测试用例**:
```typescript
// 测试文件: client/src/__tests__/Challenges.test.tsx
- test('渲染挑战列表')
- test('挑战详情页显示')
- test('任务完成状态更新')
```

---

### 迭代八：证书系统

**功能说明**：用户完成课程后获得证书。

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| 证书模板 | 模拟数据 | 固定样式 |
| 证书列表 | 真实数据 | 用户获得的证书 |
| 证书进度 | 真实数据 | 课程完成百分比 |

**完成度检查**:
- [ ] Profile 页面显示证书列表
- [ ] 每个证书显示课程名 + 进度百分比
- [ ] 锁定/解锁状态图标
- [ ] 完成课程后自动解锁证书
- [ ] 证书卡片点击（显示详情或提示）

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|---------|
| 证书列表 | 访问 Profile 页面 | 显示9个证书（锁定状态） |
| 进度显示 | 学习部分课程后 | 显示正确进度百分比 |
| 解锁证书 | 完成整个课程 | 证书状态变为解锁 |

**自动化测试用例**:
```typescript
// 测试文件: server/certificates.test.ts
- test('获取用户证书列表')
- test('证书进度计算正确')
- test('完成课程解锁证书')
```

---

### 迭代九：AI Tools Coming Soon + Profile 页面完善

**功能说明**：合并原迭代九和迭代十，完成 AI Tools Coming Soon 页面和 Profile 页面完善。

#### AI Tools Coming Soon

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| Coming Soon 页面 | UI组件 | 展示即将上线提示 |
| 导航栏入口 | 保留 | 点击进入 Coming Soon 页面 |

#### Profile 页面

| 功能 | 数据类型 | 说明 |
|-----|---------|------|
| 用户信息 | 真实数据 | 用户名、邮箱、头像 |
| Prompts Library | 模拟 | 点击提示 Coming Soon |
| 深色模式 | 真实功能 | 主题切换 |

**完成度检查**:

*AI Tools Coming Soon:*
- [ ] AI Tools 页面显示 Coming Soon UI
- [ ] 居中大标题 "AI Tools"
- [ ] 副标题 "Coming Soon"
- [ ] 简短说明文字
- [ ] 导航栏 AI Tools 链接正常工作

*Profile 页面:*
- [ ] 用户头像 + 用户名 + 邮箱显示
- [ ] "Prompts Library" 入口（点击提示 Coming Soon）
- [ ] "Help" 入口（跳转帮助页或提示）
- [ ] "Settings" 入口
- [ ] 证书列表（来自迭代八）
- [ ] 深色模式切换按钮
- [ ] "Log Out" 按钮
- [ ] 深色模式样式适配

**测试方案**:

| 测试项 | 测试方法 | 预期结果 |
|-------|---------|----------|
| AI Tools 页面 | 访问 AI Tools 页面 | 显示 Coming Soon 页面 |
| 用户信息 | 登录后访问 Profile | 显示正确用户名和邮箱 |
| 深色模式 | 点击切换按钮 | 页面切换为深色主题 |
| 登出 | 点击 Log Out | 清除登录状态，跳转首页 |

**自动化测试用例**:
```typescript
// 测试文件: client/src/__tests__/AITools.test.tsx
- test('AI Tools 页面显示 Coming Soon')
- test('导航链接正常跳转')

// 测试文件: client/src/__tests__/Profile.test.tsx
- test('显示用户信息')
- test('深色模式切换')
- test('登出功能')
```

**预估时间**：1 小时

**E2E 测试**：最终 E2E 检查（全部功能 + 完整用户旅程）

---

## 3. 迭代顺序与依赖关系

```
迭代一 (用户系统) ──┬──> 迭代二 (Streaks)
                   │
                   └──> 迭代三 (课程数据) ──> 迭代四 (路径视图) ──> 迭代五 (阅读页) ──> 迭代六 (Quiz)
                                          │
                                          └──> 迭代七 (挑战系统)
                                          │
                                          └──> 迭代八 (证书系统)

迭代九 (AI对话) ──> 独立，可并行

迭代十 (Profile) ──> 依赖迭代一、八
```

---

## 4. 数据汇总

### 模拟数据（硬编码）

| 数据类型 | 数量 | 说明 |
|---------|-----|------|
| 课程 | 9个 | ChatGPT, DALL-E, Midjourney, Claude, DeepSeek, Stable Diffusion, Gemini, Jasper AI, Lovable |
| 课程模块 | ~150个 | 每课程约15-20个模块 |
| 课程内容页 | ~500页 | 每模块约3-5页，每页200-500字 |
| Quiz 问题 | ~150个 | 每模块1-2个问题 |
| 挑战 | 4个 | 与原版一致 |
| 挑战任务 | 84个 | 28+28+14+14天 |
| AI 预设回复 | 20-30条 | 常见问题的模拟回复 |

### 真实数据（数据库存储）

| 数据类型 | 表名 | 说明 |
|---------|-----|------|
| 用户账号 | users | 邮箱、密码哈希、用户名、头像URL |
| 用户 Streaks | user_streaks | 连续学习天数、本周记录 |
| 学习进度 | user_progress | 课程/模块/内容完成状态 |
| Quiz 记录 | user_quiz_results | 答题记录 |
| 挑战进度 | user_challenges | 挑战任务完成状态 |
| 证书记录 | certificates | 已获得的证书 |

---

## 5. 预估时间

| 迭代 | 预估时间 | 累计时间 |
|-----|---------|---------|
| 迭代一：用户系统 | 1小时 | 1小时 |
| 迭代二：Streaks | 1小时 | 2小时 |
| 迭代三：课程数据 | 1.5小时 | 3.5小时 |
| 迭代四：路径视图 | 1.5小时 | 5小时 |
| 迭代五：阅读页 | 1.5小时 | 6.5小时 |
| 迭代六：Quiz | 1小时 | 7.5小时 |
| 迭代七：挑战系统 | 1.5小时 | 9小时 |
| 迭代八：证书系统 | 1小时 | 10小时 |
| 迭代九：AI对话 | 1.5小时 | 11.5小时 |
| 迭代十：Profile | 0.5小时 | 12小时 |

**总计**: 约 12 小时

---

## 6. 每次迭代完成后的检查流程

1. **代码检查**: 运行 `pnpm test` 确保所有测试通过
2. **TypeScript 检查**: 运行 `pnpm tsc --noEmit` 无错误
3. **UI 检查**: 浏览器验证页面显示正确
4. **交互检查**: 测试所有按钮、表单、导航功能
5. **数据检查**: 验证数据库读写正常
6. **更新文档**: 在本文档标记完成状态
7. **保存 Checkpoint**: 调用 `webdev_save_checkpoint`
8. **提交 GitHub**: 同步代码到仓库

---

## 7. 实施状态追踪

| 迭代 | 状态 | 开始时间 | 完成时间 | 测试通过 | 备注 |
|-----|------|---------|---------|---------|------|
| 迭代一：用户系统 | ✅ 完成 | 01:00 | 01:10 | 26 passed | 邮箱登录+测试模式 |
| 迭代二：Streaks | ✅ 完成 | 01:10 | 01:12 | 26 passed | Weekly Streaks 系统 |
| 迭代三：课程数据 | ✅ 完成 | 01:12 | 01:15 | 26 passed | 课程数据模型+API |
| 迭代四：路径视图 | ⏳ 待开始 | - | - | - | - |
| 迭代五：阅读页 | ⏳ 待开始 | - | - | - | - |
| 迭代六：Quiz | ⏳ 待开始 | - | - | - | - |
| 迭代七：挑战系统 | ⏳ 待开始 | - | - | - | - |
| 迭代八：证书系统 | ⏳ 待开始 | - | - | - | - |
| 迭代九：AI对话 | ⏳ 待开始 | - | - | - | - |
| 迭代十：Profile | ⏳ 待开始 | - | - | - | - |

---

## 8. 自动化测试汇总

| 测试文件 | 测试数量 | 覆盖功能 |
|---------|---------|---------|
| server/auth.test.ts | 4 | 用户注册、登录、登出 |
| server/streaks.test.ts | 3 | Streak 增加、获取、持久化 |
| server/courses.test.ts | 3 | 课程列表、模块、内容 |
| server/certificates.test.ts | 3 | 证书列表、进度、解锁 |
| client/CoursePathway.test.tsx | 3 | 路径树、弹窗、进度 |
| client/LessonContent.test.tsx | 4 | 内容、翻页、弹窗、进度 |
| client/Quiz.test.tsx | 3 | 问题、正确反馈、错误反馈 |
| client/Challenges.test.tsx | 3 | 列表、详情、任务状态 |
| client/AITools.test.tsx | 3 | 界面、发送、快捷按钮 |
| client/Profile.test.tsx | 3 | 用户信息、深色模式、登出 |

**总计**: 约 32 个新增测试用例

---

**文档状态**: 实施中

**开始时间**: 2026-01-11


---

## 9. E2E 浏览器自动化测试方案

### 测试工具

使用 **Playwright** 进行真实浏览器自动化测试。

### 测试执行时机

| 时机 | 说明 |
|-----|------|
| 每 3 次迭代后 | 迭代三、迭代六、迭代九完成后执行完整 E2E 测试 |
| 最终交付前 | 所有迭代完成后执行完整 E2E 测试 |

### E2E 测试执行计划

| 检查点 | 触发时机 | 测试范围 |
|-------|---------|---------|
| **E2E 检查点 1** | 迭代三完成后 | 用户系统 + Streaks + 课程数据 |
| **E2E 检查点 2** | 迭代六完成后 | 上述 + 路径视图 + 阅读页 + Quiz |
| **E2E 检查点 3** | 迭代九完成后 | 上述 + 挑战系统 + 证书 + AI对话 |
| **E2E 最终检查** | 迭代十完成后 | 全部功能完整测试 |

### E2E 测试用例清单

#### 用户系统测试 (迭代一)
```typescript
// e2e/auth.spec.ts
test('新用户完成Quiz后注册', async ({ page }) => {
  await page.goto('/quiz');
  // 完成Quiz流程
  // 输入邮箱密码
  // 验证跳转到Dashboard
});

test('已注册用户登录', async ({ page }) => {
  await page.goto('/login');
  await page.fill('[name="email"]', 'test@example.com');
  await page.fill('[name="password"]', 'password123');
  await page.click('button[type="submit"]');
  await expect(page).toHaveURL('/dashboard');
});

test('登出功能', async ({ page }) => {
  // 登录后点击登出
  // 验证跳转到首页
});
```

#### Streaks 测试 (迭代二)
```typescript
// e2e/streaks.spec.ts
test('学习后Streak增加', async ({ page }) => {
  // 登录
  // 开启测试模式
  // 完成一个课程内容
  // 验证Streak数字增加
});

test('Streak日历显示', async ({ page }) => {
  // 登录
  // 访问Dashboard Home
  // 验证Weekly Streaks卡片显示
});
```

#### 课程流程测试 (迭代三-六)
```typescript
// e2e/courses.spec.ts
test('浏览课程列表', async ({ page }) => {
  await page.goto('/dashboard/guides');
  await expect(page.locator('.course-card')).toHaveCount(9);
});

test('进入课程学习路径', async ({ page }) => {
  await page.goto('/dashboard/guides');
  await page.click('.course-card:first-child');
  await expect(page.locator('.pathway-tree')).toBeVisible();
});

test('阅读课程内容', async ({ page }) => {
  // 进入课程
  // 点击模块
  // 点击Read按钮
  // 验证内容页显示
  // 点击Continue翻页
});

test('完成Quiz', async ({ page }) => {
  // 进入Quiz页面
  // 选择答案
  // 验证反馈显示
});
```

#### 挑战系统测试 (迭代七)
```typescript
// e2e/challenges.spec.ts
test('浏览挑战列表', async ({ page }) => {
  await page.goto('/dashboard/challenges');
  await expect(page.locator('.challenge-card')).toHaveCount(4);
});

test('查看挑战详情', async ({ page }) => {
  await page.goto('/dashboard/challenges');
  await page.click('.challenge-card:first-child');
  await expect(page.locator('.day-navigation')).toBeVisible();
});
```

#### AI Tools Coming Soon 测试 (迭代九)
```typescript
// e2e/ai-tools.spec.ts
test('AI Tools 显示 Coming Soon 页面', async ({ page }) => {
  await page.goto('/dashboard/ai-tools');
  await expect(page.locator('text=Coming Soon')).toBeVisible();
});
```

#### 完整用户旅程测试 (最终检查)
```typescript
// e2e/full-journey.spec.ts
test('完整用户旅程：注册→学习→获得证书', async ({ page }) => {
  // 1. 访问首页
  await page.goto('/');
  
  // 2. 点击Get Started进入Quiz
  await page.click('text=Get Started');
  
  // 3. 完成Quiz流程
  // ...完成22步问卷
  
  // 4. 注册账号
  await page.fill('[name="email"]', 'newuser@example.com');
  await page.fill('[name="password"]', 'password123');
  await page.click('button[type="submit"]');
  
  // 5. 进入Dashboard
  await expect(page).toHaveURL('/dashboard');
  
  // 6. 开启测试模式
  await page.goto('/dashboard/profile');
  await page.click('text=Settings');
  await page.click('[data-testid="test-mode-toggle"]');
  
  // 7. 学习课程
  await page.goto('/dashboard/guides');
  await page.click('.course-card:first-child');
  await page.click('.pathway-node:first-child');
  await page.click('text=Read');
  
  // 8. 完成课程内容（测试模式下快速完成）
  await page.click('[data-testid="complete-module"]');
  
  // 9. 验证证书解锁（测试模式下10%即可）
  await page.goto('/dashboard/profile');
  await expect(page.locator('.certificate-unlocked')).toBeVisible();
});
```

### E2E 测试执行命令

```bash
# 安装 Playwright
pnpm add -D @playwright/test

# 安装浏览器
npx playwright install chromium

# 运行所有 E2E 测试
pnpm exec playwright test

# 运行特定测试文件
pnpm exec playwright test e2e/auth.spec.ts

# 以 UI 模式运行（可视化调试）
pnpm exec playwright test --ui
```

### E2E 测试配置

```typescript
// playwright.config.ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  timeout: 30000,
  use: {
    baseURL: 'http://localhost:3000',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  webServer: {
    command: 'pnpm dev',
    port: 3000,
    reuseExistingServer: true,
  },
});
```

---

## 10. 更新后的迭代流程

### 单次迭代流程

```
开始迭代
    ↓
编写代码
    ↓
运行单元测试 (pnpm test)
    ↓
    ├── 失败 → 修复 → 重新测试 (循环)
    ↓
TypeScript 检查
    ↓
    ├── 错误 → 修复 → 重新检查 (循环)
    ↓
浏览器 UI 验证
    ↓
    ├── 问题 → 修复 → 重新验证 (循环)
    ↓
更新文档状态
    ↓
保存 Checkpoint
    ↓
是否为 E2E 检查点？(迭代3/6/9/10)
    ├── 是 → 运行完整 E2E 测试
    │         ├── 失败 → 修复 → 重新测试 (循环)
    │         ↓
    │       E2E 全部通过
    ↓
进入下一迭代
```

#### E2E 检查点汇总
| 检查点 | 迭代 | E2E 测试文件 |
|-------|-----|-------------|
| 检查点 1 | 迭代三后 | auth.spec.ts, streaks.spec.ts, courses.spec.ts (部分) |
| 检查点 2 | 迭代六后 | 上述 + courses.spec.ts (完整), quiz.spec.ts |
| 最终检查 | 迭代九后 | 全部 + challenges.spec.ts, ai-tools.spec.ts, full-journey.spec.ts |

---

## 11. 更新后的预估时间

| 迭代 | 开发时间 | E2E 测试 | 累计时间 |
|-----|---------|---------|---------|
| 迭代一：用户系统 | 1小时 | - | 1小时 |
| 迭代二：Streaks | 1小时 | - | 2小时 |
| 迭代三：课程数据 | 1.5小时 | **+0.5小时 E2E** | 4小时 |
| 迭代四：路径视图 | 1.5小时 | - | 5.5小时 |
| 迭代五：阅读页 | 1.5小时 | - | 7小时 |
| 迭代六：Quiz | 1小时 | **+0.5小时 E2E** | 8.5小时 |
| 迭代七：挑战系统 | 1.5小时 | - | 10小时 |
| 迭代八：证书系统 | 1小时 | - | 11小时 |
| 迭代九：AI Tools + Profile | 1小时 | **+1小时 最终E2E** | 13小时 |

**总计**: 约 13 小时（9个迭代）

---

## 12. 实施状态追踪（更新版）

| 迭代 | 状态 | 单元测试 | E2E测试 | 备注 |
|-----|------|---------|---------|------|
| 迭代一：用户系统 | 🟡 进行中 | - | - | - |
| 迭代二：Streaks | ⏳ 待开始 | - | - | - |
| 迭代三：课程数据 | ⏳ 待开始 | - | **E2E检查点1** | - |
| 迭代四：路径视图 | ⏳ 待开始 | - | - | - |
| 迭代五：阅读页 | ⏳ 待开始 | - | - | - |
| 迭代六：Quiz | ⏳ 待开始 | - | **E2E检查点2** | - |
| 迭代七：挑战系统 | ⏳ 待开始 | - | - | - |
| 迭代八：证书系统 | ⏳ 待开始 | - | - | - |
| 迭代九：AI Tools + Profile | ⏳ 待开始 | - | **最终E2E检查** | - |

---

**文档状态**: 实施中

**开始时间**: 2026-01-11
